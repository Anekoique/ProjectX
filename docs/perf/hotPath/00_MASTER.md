# `{Feature Name}` MASTER `{NN}`

> Feature: `{feature-name}`
> Iteration: `{NN}`
> Target Plan: `{NN_PLAN.md}`

## Decision
{Final judgment for this round.}

## Directives
### M-001
- MUST: You just proposed the optimization to Icache, which is not expect, we want to perf all reset contens at PERF_DEV.md
- Reason: add more content which not only Icache.

### M-002(do immediaty, no wait to impl)
- MUST: move the content BusFastPath and Icache dir to upper layer, create dir of perf and place in the Icache and BusFastPath
  More: consider rename Icache, this stage not only perf about Icache

### M-003
- MUST: clean codes, clean organization, clean layout

## Status
- Not Approved
- Ready for Implementation: No